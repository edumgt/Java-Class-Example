# Java-Basic-005 동기화 디렉터리

이 디렉터리는 `https://github.com/edumgt/Java-Basic-005` 원본을 학습용으로 동기화해 두는 위치입니다.

동기화 명령:

```bash
bash scripts/import_java_basic_005.sh
```

> 현재 실행 환경에서 GitHub 접근이 제한되면 동기화가 실패할 수 있습니다.
